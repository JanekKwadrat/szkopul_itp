#include <bits/stdc++.h>
using namespace std;

typedef long long i64;

constexpr int N = 3e5 + 20;
int n, m;
int S[N], A[N];
bool ok = true;

ceil(s/a) <= d



int main() {

    cin >> n >> m;
    while(m--) {
        char c;
        cin >> c;
        switch(c) {
        case 'P': {
            int x, s, a;
            cin >> x >> s >> a;
            S[x] = 0;
            A[x] = 0;
            ok = false;
        } break;
        case 'U': {
            int x;
            S[x] = 0;
            A[x] = 0;
        } break
        case 'Z': {

        } break
        }
    }

    return 0;
}