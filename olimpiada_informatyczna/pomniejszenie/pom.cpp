/* Jan Zakrzewski
   XXVII Olimpiada Informatyczna, Etap I
   Zadanie `Pomniejszenie (pom)' */


#include <bits/stdc++.h>
using namespace std;

constexpr int N = 1e5 + 20;
int t, n, k;
char A[N], B[N];

int main() {

    cin >> t;
    while(t--) {
        cin >> A >> B >> k;
        n = strlen(A);
        for(int i = 0; i < n; ++i) {
            
        }
    }

    return 0;
}
