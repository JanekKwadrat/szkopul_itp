#include <bits/stdc++.h>
using namespace std;

constexpr int N = 200'050;
int n, m;
long long t[N];
long long d[N];

int main() {

    cin >> n >> m;
    for(int i = 1; i <= n; ++i) cin >> t[i];
    for(int i = 1; i <= m; ++i) cin >> d[i];


    
    return 0;
}